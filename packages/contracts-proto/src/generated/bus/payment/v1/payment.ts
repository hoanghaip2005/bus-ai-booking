// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: bus/payment/v1/payment.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  type CallOptions,
  type ChannelCredentials,
  Client,
  type ClientOptions,
  type ClientUnaryCall,
  type handleUnaryCall,
  makeGenericClientConstructor,
  type Metadata,
  type ServiceError,
  type UntypedServiceImplementation,
} from "@grpc/grpc-js";

export const protobufPackage = "bus.payment.v1";

export enum PaymentOwnerType {
  PAYMENT_OWNER_TYPE_UNSPECIFIED = 0,
  PAYMENT_OWNER_TYPE_GUEST_SESSION = 1,
  PAYMENT_OWNER_TYPE_CUSTOMER = 2,
  UNRECOGNIZED = -1,
}

export function paymentOwnerTypeFromJSON(object: any): PaymentOwnerType {
  switch (object) {
    case 0:
    case "PAYMENT_OWNER_TYPE_UNSPECIFIED":
      return PaymentOwnerType.PAYMENT_OWNER_TYPE_UNSPECIFIED;
    case 1:
    case "PAYMENT_OWNER_TYPE_GUEST_SESSION":
      return PaymentOwnerType.PAYMENT_OWNER_TYPE_GUEST_SESSION;
    case 2:
    case "PAYMENT_OWNER_TYPE_CUSTOMER":
      return PaymentOwnerType.PAYMENT_OWNER_TYPE_CUSTOMER;
    case -1:
    case "UNRECOGNIZED":
    default:
      return PaymentOwnerType.UNRECOGNIZED;
  }
}

export function paymentOwnerTypeToJSON(object: PaymentOwnerType): string {
  switch (object) {
    case PaymentOwnerType.PAYMENT_OWNER_TYPE_UNSPECIFIED:
      return "PAYMENT_OWNER_TYPE_UNSPECIFIED";
    case PaymentOwnerType.PAYMENT_OWNER_TYPE_GUEST_SESSION:
      return "PAYMENT_OWNER_TYPE_GUEST_SESSION";
    case PaymentOwnerType.PAYMENT_OWNER_TYPE_CUSTOMER:
      return "PAYMENT_OWNER_TYPE_CUSTOMER";
    case PaymentOwnerType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum RequestedPaymentOutcome {
  REQUESTED_PAYMENT_OUTCOME_UNSPECIFIED = 0,
  REQUESTED_PAYMENT_OUTCOME_SUCCESS = 1,
  REQUESTED_PAYMENT_OUTCOME_FAILURE = 2,
  UNRECOGNIZED = -1,
}

export function requestedPaymentOutcomeFromJSON(object: any): RequestedPaymentOutcome {
  switch (object) {
    case 0:
    case "REQUESTED_PAYMENT_OUTCOME_UNSPECIFIED":
      return RequestedPaymentOutcome.REQUESTED_PAYMENT_OUTCOME_UNSPECIFIED;
    case 1:
    case "REQUESTED_PAYMENT_OUTCOME_SUCCESS":
      return RequestedPaymentOutcome.REQUESTED_PAYMENT_OUTCOME_SUCCESS;
    case 2:
    case "REQUESTED_PAYMENT_OUTCOME_FAILURE":
      return RequestedPaymentOutcome.REQUESTED_PAYMENT_OUTCOME_FAILURE;
    case -1:
    case "UNRECOGNIZED":
    default:
      return RequestedPaymentOutcome.UNRECOGNIZED;
  }
}

export function requestedPaymentOutcomeToJSON(object: RequestedPaymentOutcome): string {
  switch (object) {
    case RequestedPaymentOutcome.REQUESTED_PAYMENT_OUTCOME_UNSPECIFIED:
      return "REQUESTED_PAYMENT_OUTCOME_UNSPECIFIED";
    case RequestedPaymentOutcome.REQUESTED_PAYMENT_OUTCOME_SUCCESS:
      return "REQUESTED_PAYMENT_OUTCOME_SUCCESS";
    case RequestedPaymentOutcome.REQUESTED_PAYMENT_OUTCOME_FAILURE:
      return "REQUESTED_PAYMENT_OUTCOME_FAILURE";
    case RequestedPaymentOutcome.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum PaymentAttemptStatus {
  PAYMENT_ATTEMPT_STATUS_UNSPECIFIED = 0,
  PAYMENT_ATTEMPT_STATUS_SUCCEEDED = 1,
  PAYMENT_ATTEMPT_STATUS_FAILED = 2,
  UNRECOGNIZED = -1,
}

export function paymentAttemptStatusFromJSON(object: any): PaymentAttemptStatus {
  switch (object) {
    case 0:
    case "PAYMENT_ATTEMPT_STATUS_UNSPECIFIED":
      return PaymentAttemptStatus.PAYMENT_ATTEMPT_STATUS_UNSPECIFIED;
    case 1:
    case "PAYMENT_ATTEMPT_STATUS_SUCCEEDED":
      return PaymentAttemptStatus.PAYMENT_ATTEMPT_STATUS_SUCCEEDED;
    case 2:
    case "PAYMENT_ATTEMPT_STATUS_FAILED":
      return PaymentAttemptStatus.PAYMENT_ATTEMPT_STATUS_FAILED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return PaymentAttemptStatus.UNRECOGNIZED;
  }
}

export function paymentAttemptStatusToJSON(object: PaymentAttemptStatus): string {
  switch (object) {
    case PaymentAttemptStatus.PAYMENT_ATTEMPT_STATUS_UNSPECIFIED:
      return "PAYMENT_ATTEMPT_STATUS_UNSPECIFIED";
    case PaymentAttemptStatus.PAYMENT_ATTEMPT_STATUS_SUCCEEDED:
      return "PAYMENT_ATTEMPT_STATUS_SUCCEEDED";
    case PaymentAttemptStatus.PAYMENT_ATTEMPT_STATUS_FAILED:
      return "PAYMENT_ATTEMPT_STATUS_FAILED";
    case PaymentAttemptStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface HealthRequest {
  requestId: string;
}

export interface HealthResponse {
  service: string;
  status: string;
  version: string;
  requestId: string;
  checkedAt: string;
  traceId: string;
}

export interface PaymentOwner {
  type: PaymentOwnerType;
  id: string;
}

export interface CreatePaymentAttemptRequest {
  bookingId: string;
  owner?: PaymentOwner | undefined;
  amountVnd: number;
  requestedOutcome: RequestedPaymentOutcome;
  idempotencyKey: string;
  requestId: string;
}

export interface CreatePaymentAttemptResponse {
  attempt?: PaymentAttempt | undefined;
  requestId: string;
}

export interface PaymentAttempt {
  id: string;
  bookingId: string;
  status: PaymentAttemptStatus;
  amountVnd: number;
  failureCode?: string | undefined;
  createdAt: string;
}

function createBaseHealthRequest(): HealthRequest {
  return { requestId: "" };
}

export const HealthRequest: MessageFns<HealthRequest> = {
  encode(message: HealthRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.requestId !== "") {
      writer.uint32(10).string(message.requestId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HealthRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHealthRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.requestId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HealthRequest {
    return {
      requestId: isSet(object.requestId)
        ? globalThis.String(object.requestId)
        : isSet(object.request_id)
        ? globalThis.String(object.request_id)
        : "",
    };
  },

  toJSON(message: HealthRequest): unknown {
    const obj: any = {};
    if (message.requestId !== "") {
      obj.requestId = message.requestId;
    }
    return obj;
  },

  create(base?: DeepPartial<HealthRequest>): HealthRequest {
    return HealthRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<HealthRequest>): HealthRequest {
    const message = createBaseHealthRequest();
    message.requestId = object.requestId ?? "";
    return message;
  },
};

function createBaseHealthResponse(): HealthResponse {
  return { service: "", status: "", version: "", requestId: "", checkedAt: "", traceId: "" };
}

export const HealthResponse: MessageFns<HealthResponse> = {
  encode(message: HealthResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.service !== "") {
      writer.uint32(10).string(message.service);
    }
    if (message.status !== "") {
      writer.uint32(18).string(message.status);
    }
    if (message.version !== "") {
      writer.uint32(26).string(message.version);
    }
    if (message.requestId !== "") {
      writer.uint32(34).string(message.requestId);
    }
    if (message.checkedAt !== "") {
      writer.uint32(42).string(message.checkedAt);
    }
    if (message.traceId !== "") {
      writer.uint32(50).string(message.traceId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HealthResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHealthResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.service = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.status = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.version = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.requestId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.checkedAt = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.traceId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HealthResponse {
    return {
      service: isSet(object.service) ? globalThis.String(object.service) : "",
      status: isSet(object.status) ? globalThis.String(object.status) : "",
      version: isSet(object.version) ? globalThis.String(object.version) : "",
      requestId: isSet(object.requestId)
        ? globalThis.String(object.requestId)
        : isSet(object.request_id)
        ? globalThis.String(object.request_id)
        : "",
      checkedAt: isSet(object.checkedAt)
        ? globalThis.String(object.checkedAt)
        : isSet(object.checked_at)
        ? globalThis.String(object.checked_at)
        : "",
      traceId: isSet(object.traceId)
        ? globalThis.String(object.traceId)
        : isSet(object.trace_id)
        ? globalThis.String(object.trace_id)
        : "",
    };
  },

  toJSON(message: HealthResponse): unknown {
    const obj: any = {};
    if (message.service !== "") {
      obj.service = message.service;
    }
    if (message.status !== "") {
      obj.status = message.status;
    }
    if (message.version !== "") {
      obj.version = message.version;
    }
    if (message.requestId !== "") {
      obj.requestId = message.requestId;
    }
    if (message.checkedAt !== "") {
      obj.checkedAt = message.checkedAt;
    }
    if (message.traceId !== "") {
      obj.traceId = message.traceId;
    }
    return obj;
  },

  create(base?: DeepPartial<HealthResponse>): HealthResponse {
    return HealthResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<HealthResponse>): HealthResponse {
    const message = createBaseHealthResponse();
    message.service = object.service ?? "";
    message.status = object.status ?? "";
    message.version = object.version ?? "";
    message.requestId = object.requestId ?? "";
    message.checkedAt = object.checkedAt ?? "";
    message.traceId = object.traceId ?? "";
    return message;
  },
};

function createBasePaymentOwner(): PaymentOwner {
  return { type: 0, id: "" };
}

export const PaymentOwner: MessageFns<PaymentOwner> = {
  encode(message: PaymentOwner, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.type !== 0) {
      writer.uint32(8).int32(message.type);
    }
    if (message.id !== "") {
      writer.uint32(18).string(message.id);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PaymentOwner {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePaymentOwner();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.type = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.id = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PaymentOwner {
    return {
      type: isSet(object.type) ? paymentOwnerTypeFromJSON(object.type) : 0,
      id: isSet(object.id) ? globalThis.String(object.id) : "",
    };
  },

  toJSON(message: PaymentOwner): unknown {
    const obj: any = {};
    if (message.type !== 0) {
      obj.type = paymentOwnerTypeToJSON(message.type);
    }
    if (message.id !== "") {
      obj.id = message.id;
    }
    return obj;
  },

  create(base?: DeepPartial<PaymentOwner>): PaymentOwner {
    return PaymentOwner.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PaymentOwner>): PaymentOwner {
    const message = createBasePaymentOwner();
    message.type = object.type ?? 0;
    message.id = object.id ?? "";
    return message;
  },
};

function createBaseCreatePaymentAttemptRequest(): CreatePaymentAttemptRequest {
  return { bookingId: "", owner: undefined, amountVnd: 0, requestedOutcome: 0, idempotencyKey: "", requestId: "" };
}

export const CreatePaymentAttemptRequest: MessageFns<CreatePaymentAttemptRequest> = {
  encode(message: CreatePaymentAttemptRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.bookingId !== "") {
      writer.uint32(10).string(message.bookingId);
    }
    if (message.owner !== undefined) {
      PaymentOwner.encode(message.owner, writer.uint32(18).fork()).join();
    }
    if (message.amountVnd !== 0) {
      writer.uint32(24).int32(message.amountVnd);
    }
    if (message.requestedOutcome !== 0) {
      writer.uint32(32).int32(message.requestedOutcome);
    }
    if (message.idempotencyKey !== "") {
      writer.uint32(42).string(message.idempotencyKey);
    }
    if (message.requestId !== "") {
      writer.uint32(50).string(message.requestId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreatePaymentAttemptRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreatePaymentAttemptRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.bookingId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.owner = PaymentOwner.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.amountVnd = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.requestedOutcome = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.idempotencyKey = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.requestId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreatePaymentAttemptRequest {
    return {
      bookingId: isSet(object.bookingId)
        ? globalThis.String(object.bookingId)
        : isSet(object.booking_id)
        ? globalThis.String(object.booking_id)
        : "",
      owner: isSet(object.owner) ? PaymentOwner.fromJSON(object.owner) : undefined,
      amountVnd: isSet(object.amountVnd)
        ? globalThis.Number(object.amountVnd)
        : isSet(object.amount_vnd)
        ? globalThis.Number(object.amount_vnd)
        : 0,
      requestedOutcome: isSet(object.requestedOutcome)
        ? requestedPaymentOutcomeFromJSON(object.requestedOutcome)
        : isSet(object.requested_outcome)
        ? requestedPaymentOutcomeFromJSON(object.requested_outcome)
        : 0,
      idempotencyKey: isSet(object.idempotencyKey)
        ? globalThis.String(object.idempotencyKey)
        : isSet(object.idempotency_key)
        ? globalThis.String(object.idempotency_key)
        : "",
      requestId: isSet(object.requestId)
        ? globalThis.String(object.requestId)
        : isSet(object.request_id)
        ? globalThis.String(object.request_id)
        : "",
    };
  },

  toJSON(message: CreatePaymentAttemptRequest): unknown {
    const obj: any = {};
    if (message.bookingId !== "") {
      obj.bookingId = message.bookingId;
    }
    if (message.owner !== undefined) {
      obj.owner = PaymentOwner.toJSON(message.owner);
    }
    if (message.amountVnd !== 0) {
      obj.amountVnd = Math.round(message.amountVnd);
    }
    if (message.requestedOutcome !== 0) {
      obj.requestedOutcome = requestedPaymentOutcomeToJSON(message.requestedOutcome);
    }
    if (message.idempotencyKey !== "") {
      obj.idempotencyKey = message.idempotencyKey;
    }
    if (message.requestId !== "") {
      obj.requestId = message.requestId;
    }
    return obj;
  },

  create(base?: DeepPartial<CreatePaymentAttemptRequest>): CreatePaymentAttemptRequest {
    return CreatePaymentAttemptRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CreatePaymentAttemptRequest>): CreatePaymentAttemptRequest {
    const message = createBaseCreatePaymentAttemptRequest();
    message.bookingId = object.bookingId ?? "";
    message.owner = (object.owner !== undefined && object.owner !== null)
      ? PaymentOwner.fromPartial(object.owner)
      : undefined;
    message.amountVnd = object.amountVnd ?? 0;
    message.requestedOutcome = object.requestedOutcome ?? 0;
    message.idempotencyKey = object.idempotencyKey ?? "";
    message.requestId = object.requestId ?? "";
    return message;
  },
};

function createBaseCreatePaymentAttemptResponse(): CreatePaymentAttemptResponse {
  return { attempt: undefined, requestId: "" };
}

export const CreatePaymentAttemptResponse: MessageFns<CreatePaymentAttemptResponse> = {
  encode(message: CreatePaymentAttemptResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.attempt !== undefined) {
      PaymentAttempt.encode(message.attempt, writer.uint32(10).fork()).join();
    }
    if (message.requestId !== "") {
      writer.uint32(18).string(message.requestId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreatePaymentAttemptResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreatePaymentAttemptResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.attempt = PaymentAttempt.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.requestId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreatePaymentAttemptResponse {
    return {
      attempt: isSet(object.attempt) ? PaymentAttempt.fromJSON(object.attempt) : undefined,
      requestId: isSet(object.requestId)
        ? globalThis.String(object.requestId)
        : isSet(object.request_id)
        ? globalThis.String(object.request_id)
        : "",
    };
  },

  toJSON(message: CreatePaymentAttemptResponse): unknown {
    const obj: any = {};
    if (message.attempt !== undefined) {
      obj.attempt = PaymentAttempt.toJSON(message.attempt);
    }
    if (message.requestId !== "") {
      obj.requestId = message.requestId;
    }
    return obj;
  },

  create(base?: DeepPartial<CreatePaymentAttemptResponse>): CreatePaymentAttemptResponse {
    return CreatePaymentAttemptResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CreatePaymentAttemptResponse>): CreatePaymentAttemptResponse {
    const message = createBaseCreatePaymentAttemptResponse();
    message.attempt = (object.attempt !== undefined && object.attempt !== null)
      ? PaymentAttempt.fromPartial(object.attempt)
      : undefined;
    message.requestId = object.requestId ?? "";
    return message;
  },
};

function createBasePaymentAttempt(): PaymentAttempt {
  return { id: "", bookingId: "", status: 0, amountVnd: 0, failureCode: undefined, createdAt: "" };
}

export const PaymentAttempt: MessageFns<PaymentAttempt> = {
  encode(message: PaymentAttempt, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.bookingId !== "") {
      writer.uint32(18).string(message.bookingId);
    }
    if (message.status !== 0) {
      writer.uint32(24).int32(message.status);
    }
    if (message.amountVnd !== 0) {
      writer.uint32(32).int32(message.amountVnd);
    }
    if (message.failureCode !== undefined) {
      writer.uint32(42).string(message.failureCode);
    }
    if (message.createdAt !== "") {
      writer.uint32(50).string(message.createdAt);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PaymentAttempt {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePaymentAttempt();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.bookingId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.amountVnd = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.failureCode = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.createdAt = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PaymentAttempt {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      bookingId: isSet(object.bookingId)
        ? globalThis.String(object.bookingId)
        : isSet(object.booking_id)
        ? globalThis.String(object.booking_id)
        : "",
      status: isSet(object.status) ? paymentAttemptStatusFromJSON(object.status) : 0,
      amountVnd: isSet(object.amountVnd)
        ? globalThis.Number(object.amountVnd)
        : isSet(object.amount_vnd)
        ? globalThis.Number(object.amount_vnd)
        : 0,
      failureCode: isSet(object.failureCode)
        ? globalThis.String(object.failureCode)
        : isSet(object.failure_code)
        ? globalThis.String(object.failure_code)
        : undefined,
      createdAt: isSet(object.createdAt)
        ? globalThis.String(object.createdAt)
        : isSet(object.created_at)
        ? globalThis.String(object.created_at)
        : "",
    };
  },

  toJSON(message: PaymentAttempt): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.bookingId !== "") {
      obj.bookingId = message.bookingId;
    }
    if (message.status !== 0) {
      obj.status = paymentAttemptStatusToJSON(message.status);
    }
    if (message.amountVnd !== 0) {
      obj.amountVnd = Math.round(message.amountVnd);
    }
    if (message.failureCode !== undefined) {
      obj.failureCode = message.failureCode;
    }
    if (message.createdAt !== "") {
      obj.createdAt = message.createdAt;
    }
    return obj;
  },

  create(base?: DeepPartial<PaymentAttempt>): PaymentAttempt {
    return PaymentAttempt.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PaymentAttempt>): PaymentAttempt {
    const message = createBasePaymentAttempt();
    message.id = object.id ?? "";
    message.bookingId = object.bookingId ?? "";
    message.status = object.status ?? 0;
    message.amountVnd = object.amountVnd ?? 0;
    message.failureCode = object.failureCode ?? undefined;
    message.createdAt = object.createdAt ?? "";
    return message;
  },
};

export type PaymentServiceService = typeof PaymentServiceService;
export const PaymentServiceService = {
  health: {
    path: "/bus.payment.v1.PaymentService/Health" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: HealthRequest): Buffer => Buffer.from(HealthRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): HealthRequest => HealthRequest.decode(value),
    responseSerialize: (value: HealthResponse): Buffer => Buffer.from(HealthResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): HealthResponse => HealthResponse.decode(value),
  },
  createPaymentAttempt: {
    path: "/bus.payment.v1.PaymentService/CreatePaymentAttempt" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: CreatePaymentAttemptRequest): Buffer =>
      Buffer.from(CreatePaymentAttemptRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): CreatePaymentAttemptRequest => CreatePaymentAttemptRequest.decode(value),
    responseSerialize: (value: CreatePaymentAttemptResponse): Buffer =>
      Buffer.from(CreatePaymentAttemptResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): CreatePaymentAttemptResponse => CreatePaymentAttemptResponse.decode(value),
  },
} as const;

export interface PaymentServiceServer extends UntypedServiceImplementation {
  health: handleUnaryCall<HealthRequest, HealthResponse>;
  createPaymentAttempt: handleUnaryCall<CreatePaymentAttemptRequest, CreatePaymentAttemptResponse>;
}

export interface PaymentServiceClient extends Client {
  health(
    request: HealthRequest,
    callback: (error: ServiceError | null, response: HealthResponse) => void,
  ): ClientUnaryCall;
  health(
    request: HealthRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: HealthResponse) => void,
  ): ClientUnaryCall;
  health(
    request: HealthRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: HealthResponse) => void,
  ): ClientUnaryCall;
  createPaymentAttempt(
    request: CreatePaymentAttemptRequest,
    callback: (error: ServiceError | null, response: CreatePaymentAttemptResponse) => void,
  ): ClientUnaryCall;
  createPaymentAttempt(
    request: CreatePaymentAttemptRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: CreatePaymentAttemptResponse) => void,
  ): ClientUnaryCall;
  createPaymentAttempt(
    request: CreatePaymentAttemptRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: CreatePaymentAttemptResponse) => void,
  ): ClientUnaryCall;
}

export const PaymentServiceClient = makeGenericClientConstructor(
  PaymentServiceService,
  "bus.payment.v1.PaymentService",
) as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): PaymentServiceClient;
  service: typeof PaymentServiceService;
  serviceName: string;
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
