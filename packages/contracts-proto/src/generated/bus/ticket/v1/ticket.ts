// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: bus/ticket/v1/ticket.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  type CallOptions,
  type ChannelCredentials,
  Client,
  type ClientOptions,
  type ClientUnaryCall,
  type handleUnaryCall,
  makeGenericClientConstructor,
  type Metadata,
  type ServiceError,
  type UntypedServiceImplementation,
} from "@grpc/grpc-js";

export const protobufPackage = "bus.ticket.v1";

export enum TicketOwnerType {
  TICKET_OWNER_TYPE_UNSPECIFIED = 0,
  TICKET_OWNER_TYPE_GUEST_SESSION = 1,
  TICKET_OWNER_TYPE_CUSTOMER = 2,
  UNRECOGNIZED = -1,
}

export function ticketOwnerTypeFromJSON(object: any): TicketOwnerType {
  switch (object) {
    case 0:
    case "TICKET_OWNER_TYPE_UNSPECIFIED":
      return TicketOwnerType.TICKET_OWNER_TYPE_UNSPECIFIED;
    case 1:
    case "TICKET_OWNER_TYPE_GUEST_SESSION":
      return TicketOwnerType.TICKET_OWNER_TYPE_GUEST_SESSION;
    case 2:
    case "TICKET_OWNER_TYPE_CUSTOMER":
      return TicketOwnerType.TICKET_OWNER_TYPE_CUSTOMER;
    case -1:
    case "UNRECOGNIZED":
    default:
      return TicketOwnerType.UNRECOGNIZED;
  }
}

export function ticketOwnerTypeToJSON(object: TicketOwnerType): string {
  switch (object) {
    case TicketOwnerType.TICKET_OWNER_TYPE_UNSPECIFIED:
      return "TICKET_OWNER_TYPE_UNSPECIFIED";
    case TicketOwnerType.TICKET_OWNER_TYPE_GUEST_SESSION:
      return "TICKET_OWNER_TYPE_GUEST_SESSION";
    case TicketOwnerType.TICKET_OWNER_TYPE_CUSTOMER:
      return "TICKET_OWNER_TYPE_CUSTOMER";
    case TicketOwnerType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface HealthRequest {
  requestId: string;
}

export interface HealthResponse {
  service: string;
  status: string;
  version: string;
  requestId: string;
  checkedAt: string;
  traceId: string;
}

export interface TicketOwner {
  type: TicketOwnerType;
  id: string;
}

export interface ListBookingTicketsRequest {
  bookingId: string;
  owner?: TicketOwner | undefined;
  requestId: string;
}

export interface ListBookingTicketsResponse {
  bookingId: string;
  ready: boolean;
  tickets: TicketDocument[];
  requestId: string;
}

export interface TicketDocument {
  id: string;
  ticketCode: string;
  bookingCode: string;
  passengerName: string;
  seatId: string;
  routeLabel: string;
  pickupName: string;
  dropoffName: string;
  departureAt: string;
  vehicleLabel: string;
  qrPayload: string;
  htmlContent: string;
  pdfDocument: Uint8Array;
  issuedAt: string;
}

function createBaseHealthRequest(): HealthRequest {
  return { requestId: "" };
}

export const HealthRequest: MessageFns<HealthRequest> = {
  encode(message: HealthRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.requestId !== "") {
      writer.uint32(10).string(message.requestId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HealthRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHealthRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.requestId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HealthRequest {
    return {
      requestId: isSet(object.requestId)
        ? globalThis.String(object.requestId)
        : isSet(object.request_id)
        ? globalThis.String(object.request_id)
        : "",
    };
  },

  toJSON(message: HealthRequest): unknown {
    const obj: any = {};
    if (message.requestId !== "") {
      obj.requestId = message.requestId;
    }
    return obj;
  },

  create(base?: DeepPartial<HealthRequest>): HealthRequest {
    return HealthRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<HealthRequest>): HealthRequest {
    const message = createBaseHealthRequest();
    message.requestId = object.requestId ?? "";
    return message;
  },
};

function createBaseHealthResponse(): HealthResponse {
  return { service: "", status: "", version: "", requestId: "", checkedAt: "", traceId: "" };
}

export const HealthResponse: MessageFns<HealthResponse> = {
  encode(message: HealthResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.service !== "") {
      writer.uint32(10).string(message.service);
    }
    if (message.status !== "") {
      writer.uint32(18).string(message.status);
    }
    if (message.version !== "") {
      writer.uint32(26).string(message.version);
    }
    if (message.requestId !== "") {
      writer.uint32(34).string(message.requestId);
    }
    if (message.checkedAt !== "") {
      writer.uint32(42).string(message.checkedAt);
    }
    if (message.traceId !== "") {
      writer.uint32(50).string(message.traceId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HealthResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHealthResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.service = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.status = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.version = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.requestId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.checkedAt = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.traceId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HealthResponse {
    return {
      service: isSet(object.service) ? globalThis.String(object.service) : "",
      status: isSet(object.status) ? globalThis.String(object.status) : "",
      version: isSet(object.version) ? globalThis.String(object.version) : "",
      requestId: isSet(object.requestId)
        ? globalThis.String(object.requestId)
        : isSet(object.request_id)
        ? globalThis.String(object.request_id)
        : "",
      checkedAt: isSet(object.checkedAt)
        ? globalThis.String(object.checkedAt)
        : isSet(object.checked_at)
        ? globalThis.String(object.checked_at)
        : "",
      traceId: isSet(object.traceId)
        ? globalThis.String(object.traceId)
        : isSet(object.trace_id)
        ? globalThis.String(object.trace_id)
        : "",
    };
  },

  toJSON(message: HealthResponse): unknown {
    const obj: any = {};
    if (message.service !== "") {
      obj.service = message.service;
    }
    if (message.status !== "") {
      obj.status = message.status;
    }
    if (message.version !== "") {
      obj.version = message.version;
    }
    if (message.requestId !== "") {
      obj.requestId = message.requestId;
    }
    if (message.checkedAt !== "") {
      obj.checkedAt = message.checkedAt;
    }
    if (message.traceId !== "") {
      obj.traceId = message.traceId;
    }
    return obj;
  },

  create(base?: DeepPartial<HealthResponse>): HealthResponse {
    return HealthResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<HealthResponse>): HealthResponse {
    const message = createBaseHealthResponse();
    message.service = object.service ?? "";
    message.status = object.status ?? "";
    message.version = object.version ?? "";
    message.requestId = object.requestId ?? "";
    message.checkedAt = object.checkedAt ?? "";
    message.traceId = object.traceId ?? "";
    return message;
  },
};

function createBaseTicketOwner(): TicketOwner {
  return { type: 0, id: "" };
}

export const TicketOwner: MessageFns<TicketOwner> = {
  encode(message: TicketOwner, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.type !== 0) {
      writer.uint32(8).int32(message.type);
    }
    if (message.id !== "") {
      writer.uint32(18).string(message.id);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TicketOwner {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTicketOwner();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.type = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.id = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TicketOwner {
    return {
      type: isSet(object.type) ? ticketOwnerTypeFromJSON(object.type) : 0,
      id: isSet(object.id) ? globalThis.String(object.id) : "",
    };
  },

  toJSON(message: TicketOwner): unknown {
    const obj: any = {};
    if (message.type !== 0) {
      obj.type = ticketOwnerTypeToJSON(message.type);
    }
    if (message.id !== "") {
      obj.id = message.id;
    }
    return obj;
  },

  create(base?: DeepPartial<TicketOwner>): TicketOwner {
    return TicketOwner.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TicketOwner>): TicketOwner {
    const message = createBaseTicketOwner();
    message.type = object.type ?? 0;
    message.id = object.id ?? "";
    return message;
  },
};

function createBaseListBookingTicketsRequest(): ListBookingTicketsRequest {
  return { bookingId: "", owner: undefined, requestId: "" };
}

export const ListBookingTicketsRequest: MessageFns<ListBookingTicketsRequest> = {
  encode(message: ListBookingTicketsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.bookingId !== "") {
      writer.uint32(10).string(message.bookingId);
    }
    if (message.owner !== undefined) {
      TicketOwner.encode(message.owner, writer.uint32(18).fork()).join();
    }
    if (message.requestId !== "") {
      writer.uint32(26).string(message.requestId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListBookingTicketsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListBookingTicketsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.bookingId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.owner = TicketOwner.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.requestId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListBookingTicketsRequest {
    return {
      bookingId: isSet(object.bookingId)
        ? globalThis.String(object.bookingId)
        : isSet(object.booking_id)
        ? globalThis.String(object.booking_id)
        : "",
      owner: isSet(object.owner) ? TicketOwner.fromJSON(object.owner) : undefined,
      requestId: isSet(object.requestId)
        ? globalThis.String(object.requestId)
        : isSet(object.request_id)
        ? globalThis.String(object.request_id)
        : "",
    };
  },

  toJSON(message: ListBookingTicketsRequest): unknown {
    const obj: any = {};
    if (message.bookingId !== "") {
      obj.bookingId = message.bookingId;
    }
    if (message.owner !== undefined) {
      obj.owner = TicketOwner.toJSON(message.owner);
    }
    if (message.requestId !== "") {
      obj.requestId = message.requestId;
    }
    return obj;
  },

  create(base?: DeepPartial<ListBookingTicketsRequest>): ListBookingTicketsRequest {
    return ListBookingTicketsRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListBookingTicketsRequest>): ListBookingTicketsRequest {
    const message = createBaseListBookingTicketsRequest();
    message.bookingId = object.bookingId ?? "";
    message.owner = (object.owner !== undefined && object.owner !== null)
      ? TicketOwner.fromPartial(object.owner)
      : undefined;
    message.requestId = object.requestId ?? "";
    return message;
  },
};

function createBaseListBookingTicketsResponse(): ListBookingTicketsResponse {
  return { bookingId: "", ready: false, tickets: [], requestId: "" };
}

export const ListBookingTicketsResponse: MessageFns<ListBookingTicketsResponse> = {
  encode(message: ListBookingTicketsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.bookingId !== "") {
      writer.uint32(10).string(message.bookingId);
    }
    if (message.ready !== false) {
      writer.uint32(16).bool(message.ready);
    }
    for (const v of message.tickets) {
      TicketDocument.encode(v!, writer.uint32(26).fork()).join();
    }
    if (message.requestId !== "") {
      writer.uint32(34).string(message.requestId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListBookingTicketsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListBookingTicketsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.bookingId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.ready = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.tickets.push(TicketDocument.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.requestId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListBookingTicketsResponse {
    return {
      bookingId: isSet(object.bookingId)
        ? globalThis.String(object.bookingId)
        : isSet(object.booking_id)
        ? globalThis.String(object.booking_id)
        : "",
      ready: isSet(object.ready) ? globalThis.Boolean(object.ready) : false,
      tickets: globalThis.Array.isArray(object?.tickets)
        ? object.tickets.map((e: any) => TicketDocument.fromJSON(e))
        : [],
      requestId: isSet(object.requestId)
        ? globalThis.String(object.requestId)
        : isSet(object.request_id)
        ? globalThis.String(object.request_id)
        : "",
    };
  },

  toJSON(message: ListBookingTicketsResponse): unknown {
    const obj: any = {};
    if (message.bookingId !== "") {
      obj.bookingId = message.bookingId;
    }
    if (message.ready !== false) {
      obj.ready = message.ready;
    }
    if (message.tickets?.length) {
      obj.tickets = message.tickets.map((e) => TicketDocument.toJSON(e));
    }
    if (message.requestId !== "") {
      obj.requestId = message.requestId;
    }
    return obj;
  },

  create(base?: DeepPartial<ListBookingTicketsResponse>): ListBookingTicketsResponse {
    return ListBookingTicketsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListBookingTicketsResponse>): ListBookingTicketsResponse {
    const message = createBaseListBookingTicketsResponse();
    message.bookingId = object.bookingId ?? "";
    message.ready = object.ready ?? false;
    message.tickets = object.tickets?.map((e) => TicketDocument.fromPartial(e)) || [];
    message.requestId = object.requestId ?? "";
    return message;
  },
};

function createBaseTicketDocument(): TicketDocument {
  return {
    id: "",
    ticketCode: "",
    bookingCode: "",
    passengerName: "",
    seatId: "",
    routeLabel: "",
    pickupName: "",
    dropoffName: "",
    departureAt: "",
    vehicleLabel: "",
    qrPayload: "",
    htmlContent: "",
    pdfDocument: new Uint8Array(0),
    issuedAt: "",
  };
}

export const TicketDocument: MessageFns<TicketDocument> = {
  encode(message: TicketDocument, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.ticketCode !== "") {
      writer.uint32(18).string(message.ticketCode);
    }
    if (message.bookingCode !== "") {
      writer.uint32(26).string(message.bookingCode);
    }
    if (message.passengerName !== "") {
      writer.uint32(34).string(message.passengerName);
    }
    if (message.seatId !== "") {
      writer.uint32(42).string(message.seatId);
    }
    if (message.routeLabel !== "") {
      writer.uint32(50).string(message.routeLabel);
    }
    if (message.pickupName !== "") {
      writer.uint32(58).string(message.pickupName);
    }
    if (message.dropoffName !== "") {
      writer.uint32(66).string(message.dropoffName);
    }
    if (message.departureAt !== "") {
      writer.uint32(74).string(message.departureAt);
    }
    if (message.vehicleLabel !== "") {
      writer.uint32(82).string(message.vehicleLabel);
    }
    if (message.qrPayload !== "") {
      writer.uint32(90).string(message.qrPayload);
    }
    if (message.htmlContent !== "") {
      writer.uint32(98).string(message.htmlContent);
    }
    if (message.pdfDocument.length !== 0) {
      writer.uint32(106).bytes(message.pdfDocument);
    }
    if (message.issuedAt !== "") {
      writer.uint32(114).string(message.issuedAt);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TicketDocument {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTicketDocument();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.ticketCode = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.bookingCode = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.passengerName = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.seatId = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.routeLabel = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.pickupName = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.dropoffName = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.departureAt = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.vehicleLabel = reader.string();
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.qrPayload = reader.string();
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.htmlContent = reader.string();
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.pdfDocument = reader.bytes();
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.issuedAt = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TicketDocument {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      ticketCode: isSet(object.ticketCode)
        ? globalThis.String(object.ticketCode)
        : isSet(object.ticket_code)
        ? globalThis.String(object.ticket_code)
        : "",
      bookingCode: isSet(object.bookingCode)
        ? globalThis.String(object.bookingCode)
        : isSet(object.booking_code)
        ? globalThis.String(object.booking_code)
        : "",
      passengerName: isSet(object.passengerName)
        ? globalThis.String(object.passengerName)
        : isSet(object.passenger_name)
        ? globalThis.String(object.passenger_name)
        : "",
      seatId: isSet(object.seatId)
        ? globalThis.String(object.seatId)
        : isSet(object.seat_id)
        ? globalThis.String(object.seat_id)
        : "",
      routeLabel: isSet(object.routeLabel)
        ? globalThis.String(object.routeLabel)
        : isSet(object.route_label)
        ? globalThis.String(object.route_label)
        : "",
      pickupName: isSet(object.pickupName)
        ? globalThis.String(object.pickupName)
        : isSet(object.pickup_name)
        ? globalThis.String(object.pickup_name)
        : "",
      dropoffName: isSet(object.dropoffName)
        ? globalThis.String(object.dropoffName)
        : isSet(object.dropoff_name)
        ? globalThis.String(object.dropoff_name)
        : "",
      departureAt: isSet(object.departureAt)
        ? globalThis.String(object.departureAt)
        : isSet(object.departure_at)
        ? globalThis.String(object.departure_at)
        : "",
      vehicleLabel: isSet(object.vehicleLabel)
        ? globalThis.String(object.vehicleLabel)
        : isSet(object.vehicle_label)
        ? globalThis.String(object.vehicle_label)
        : "",
      qrPayload: isSet(object.qrPayload)
        ? globalThis.String(object.qrPayload)
        : isSet(object.qr_payload)
        ? globalThis.String(object.qr_payload)
        : "",
      htmlContent: isSet(object.htmlContent)
        ? globalThis.String(object.htmlContent)
        : isSet(object.html_content)
        ? globalThis.String(object.html_content)
        : "",
      pdfDocument: isSet(object.pdfDocument)
        ? bytesFromBase64(object.pdfDocument)
        : isSet(object.pdf_document)
        ? bytesFromBase64(object.pdf_document)
        : new Uint8Array(0),
      issuedAt: isSet(object.issuedAt)
        ? globalThis.String(object.issuedAt)
        : isSet(object.issued_at)
        ? globalThis.String(object.issued_at)
        : "",
    };
  },

  toJSON(message: TicketDocument): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.ticketCode !== "") {
      obj.ticketCode = message.ticketCode;
    }
    if (message.bookingCode !== "") {
      obj.bookingCode = message.bookingCode;
    }
    if (message.passengerName !== "") {
      obj.passengerName = message.passengerName;
    }
    if (message.seatId !== "") {
      obj.seatId = message.seatId;
    }
    if (message.routeLabel !== "") {
      obj.routeLabel = message.routeLabel;
    }
    if (message.pickupName !== "") {
      obj.pickupName = message.pickupName;
    }
    if (message.dropoffName !== "") {
      obj.dropoffName = message.dropoffName;
    }
    if (message.departureAt !== "") {
      obj.departureAt = message.departureAt;
    }
    if (message.vehicleLabel !== "") {
      obj.vehicleLabel = message.vehicleLabel;
    }
    if (message.qrPayload !== "") {
      obj.qrPayload = message.qrPayload;
    }
    if (message.htmlContent !== "") {
      obj.htmlContent = message.htmlContent;
    }
    if (message.pdfDocument.length !== 0) {
      obj.pdfDocument = base64FromBytes(message.pdfDocument);
    }
    if (message.issuedAt !== "") {
      obj.issuedAt = message.issuedAt;
    }
    return obj;
  },

  create(base?: DeepPartial<TicketDocument>): TicketDocument {
    return TicketDocument.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TicketDocument>): TicketDocument {
    const message = createBaseTicketDocument();
    message.id = object.id ?? "";
    message.ticketCode = object.ticketCode ?? "";
    message.bookingCode = object.bookingCode ?? "";
    message.passengerName = object.passengerName ?? "";
    message.seatId = object.seatId ?? "";
    message.routeLabel = object.routeLabel ?? "";
    message.pickupName = object.pickupName ?? "";
    message.dropoffName = object.dropoffName ?? "";
    message.departureAt = object.departureAt ?? "";
    message.vehicleLabel = object.vehicleLabel ?? "";
    message.qrPayload = object.qrPayload ?? "";
    message.htmlContent = object.htmlContent ?? "";
    message.pdfDocument = object.pdfDocument ?? new Uint8Array(0);
    message.issuedAt = object.issuedAt ?? "";
    return message;
  },
};

export type TicketServiceService = typeof TicketServiceService;
export const TicketServiceService = {
  health: {
    path: "/bus.ticket.v1.TicketService/Health" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: HealthRequest): Buffer => Buffer.from(HealthRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): HealthRequest => HealthRequest.decode(value),
    responseSerialize: (value: HealthResponse): Buffer => Buffer.from(HealthResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): HealthResponse => HealthResponse.decode(value),
  },
  listBookingTickets: {
    path: "/bus.ticket.v1.TicketService/ListBookingTickets" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ListBookingTicketsRequest): Buffer =>
      Buffer.from(ListBookingTicketsRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ListBookingTicketsRequest => ListBookingTicketsRequest.decode(value),
    responseSerialize: (value: ListBookingTicketsResponse): Buffer =>
      Buffer.from(ListBookingTicketsResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ListBookingTicketsResponse => ListBookingTicketsResponse.decode(value),
  },
} as const;

export interface TicketServiceServer extends UntypedServiceImplementation {
  health: handleUnaryCall<HealthRequest, HealthResponse>;
  listBookingTickets: handleUnaryCall<ListBookingTicketsRequest, ListBookingTicketsResponse>;
}

export interface TicketServiceClient extends Client {
  health(
    request: HealthRequest,
    callback: (error: ServiceError | null, response: HealthResponse) => void,
  ): ClientUnaryCall;
  health(
    request: HealthRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: HealthResponse) => void,
  ): ClientUnaryCall;
  health(
    request: HealthRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: HealthResponse) => void,
  ): ClientUnaryCall;
  listBookingTickets(
    request: ListBookingTicketsRequest,
    callback: (error: ServiceError | null, response: ListBookingTicketsResponse) => void,
  ): ClientUnaryCall;
  listBookingTickets(
    request: ListBookingTicketsRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ListBookingTicketsResponse) => void,
  ): ClientUnaryCall;
  listBookingTickets(
    request: ListBookingTicketsRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ListBookingTicketsResponse) => void,
  ): ClientUnaryCall;
}

export const TicketServiceClient = makeGenericClientConstructor(
  TicketServiceService,
  "bus.ticket.v1.TicketService",
) as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): TicketServiceClient;
  service: typeof TicketServiceService;
  serviceName: string;
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
