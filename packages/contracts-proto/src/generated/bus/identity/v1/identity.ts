// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: bus/identity/v1/identity.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  type CallOptions,
  type ChannelCredentials,
  Client,
  type ClientOptions,
  type ClientUnaryCall,
  type handleUnaryCall,
  makeGenericClientConstructor,
  type Metadata,
  type ServiceError,
  type UntypedServiceImplementation,
} from "@grpc/grpc-js";

export const protobufPackage = "bus.identity.v1";

export enum IdentityRole {
  IDENTITY_ROLE_UNSPECIFIED = 0,
  IDENTITY_ROLE_CUSTOMER = 1,
  IDENTITY_ROLE_STAFF = 2,
  IDENTITY_ROLE_ADMIN = 3,
  UNRECOGNIZED = -1,
}

export function identityRoleFromJSON(object: any): IdentityRole {
  switch (object) {
    case 0:
    case "IDENTITY_ROLE_UNSPECIFIED":
      return IdentityRole.IDENTITY_ROLE_UNSPECIFIED;
    case 1:
    case "IDENTITY_ROLE_CUSTOMER":
      return IdentityRole.IDENTITY_ROLE_CUSTOMER;
    case 2:
    case "IDENTITY_ROLE_STAFF":
      return IdentityRole.IDENTITY_ROLE_STAFF;
    case 3:
    case "IDENTITY_ROLE_ADMIN":
      return IdentityRole.IDENTITY_ROLE_ADMIN;
    case -1:
    case "UNRECOGNIZED":
    default:
      return IdentityRole.UNRECOGNIZED;
  }
}

export function identityRoleToJSON(object: IdentityRole): string {
  switch (object) {
    case IdentityRole.IDENTITY_ROLE_UNSPECIFIED:
      return "IDENTITY_ROLE_UNSPECIFIED";
    case IdentityRole.IDENTITY_ROLE_CUSTOMER:
      return "IDENTITY_ROLE_CUSTOMER";
    case IdentityRole.IDENTITY_ROLE_STAFF:
      return "IDENTITY_ROLE_STAFF";
    case IdentityRole.IDENTITY_ROLE_ADMIN:
      return "IDENTITY_ROLE_ADMIN";
    case IdentityRole.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface HealthRequest {
  requestId: string;
}

export interface HealthResponse {
  service: string;
  status: string;
  version: string;
  requestId: string;
  traceId: string;
  checkedAt: string;
}

export interface IdentityUser {
  id: string;
  email: string;
  displayName: string;
  role: IdentityRole;
}

export interface AuthSession {
  accessToken: string;
  refreshToken: string;
  accessExpiresAt: string;
  refreshExpiresAt: string;
  user?: IdentityUser | undefined;
}

export interface LoginRequest {
  email: string;
  password: string;
  requestId: string;
}

export interface RefreshRequest {
  refreshToken: string;
  requestId: string;
}

export interface LoginResponse {
  session?: AuthSession | undefined;
  requestId: string;
}

export interface RefreshResponse {
  session?: AuthSession | undefined;
  requestId: string;
}

export interface LogoutRequest {
  refreshToken: string;
  requestId: string;
}

export interface LogoutResponse {
  revoked: boolean;
  requestId: string;
}

export interface ValidateAccessTokenRequest {
  accessToken: string;
  requestId: string;
}

export interface AuthenticatedActor {
  id: string;
  email: string;
  displayName: string;
  role: IdentityRole;
  tokenId: string;
  expiresAt: string;
}

export interface ValidateAccessTokenResponse {
  actor?: AuthenticatedActor | undefined;
  requestId: string;
}

export interface PassengerProfile {
  id: string;
  label: string;
  fullName: string;
  phone?: string | undefined;
  createdAt: string;
  updatedAt: string;
}

export interface PassengerProfileInput {
  label: string;
  fullName: string;
  phone?: string | undefined;
}

export interface ListPassengerProfilesRequest {
  requestId: string;
}

export interface ListPassengerProfilesResponse {
  profiles: PassengerProfile[];
  requestId: string;
}

export interface CreatePassengerProfileRequest {
  input?: PassengerProfileInput | undefined;
  requestId: string;
}

export interface UpdatePassengerProfileRequest {
  profileId: string;
  input?: PassengerProfileInput | undefined;
  requestId: string;
}

export interface CreatePassengerProfileResponse {
  profile?: PassengerProfile | undefined;
  requestId: string;
}

export interface UpdatePassengerProfileResponse {
  profile?: PassengerProfile | undefined;
  requestId: string;
}

export interface DeletePassengerProfileRequest {
  profileId: string;
  requestId: string;
}

export interface DeletePassengerProfileResponse {
  profileId: string;
  deleted: boolean;
  requestId: string;
}

function createBaseHealthRequest(): HealthRequest {
  return { requestId: "" };
}

export const HealthRequest: MessageFns<HealthRequest> = {
  encode(message: HealthRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.requestId !== "") {
      writer.uint32(10).string(message.requestId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HealthRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHealthRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.requestId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HealthRequest {
    return {
      requestId: isSet(object.requestId)
        ? globalThis.String(object.requestId)
        : isSet(object.request_id)
        ? globalThis.String(object.request_id)
        : "",
    };
  },

  toJSON(message: HealthRequest): unknown {
    const obj: any = {};
    if (message.requestId !== "") {
      obj.requestId = message.requestId;
    }
    return obj;
  },

  create(base?: DeepPartial<HealthRequest>): HealthRequest {
    return HealthRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<HealthRequest>): HealthRequest {
    const message = createBaseHealthRequest();
    message.requestId = object.requestId ?? "";
    return message;
  },
};

function createBaseHealthResponse(): HealthResponse {
  return { service: "", status: "", version: "", requestId: "", traceId: "", checkedAt: "" };
}

export const HealthResponse: MessageFns<HealthResponse> = {
  encode(message: HealthResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.service !== "") {
      writer.uint32(10).string(message.service);
    }
    if (message.status !== "") {
      writer.uint32(18).string(message.status);
    }
    if (message.version !== "") {
      writer.uint32(26).string(message.version);
    }
    if (message.requestId !== "") {
      writer.uint32(34).string(message.requestId);
    }
    if (message.traceId !== "") {
      writer.uint32(42).string(message.traceId);
    }
    if (message.checkedAt !== "") {
      writer.uint32(50).string(message.checkedAt);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HealthResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHealthResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.service = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.status = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.version = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.requestId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.traceId = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.checkedAt = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HealthResponse {
    return {
      service: isSet(object.service) ? globalThis.String(object.service) : "",
      status: isSet(object.status) ? globalThis.String(object.status) : "",
      version: isSet(object.version) ? globalThis.String(object.version) : "",
      requestId: isSet(object.requestId)
        ? globalThis.String(object.requestId)
        : isSet(object.request_id)
        ? globalThis.String(object.request_id)
        : "",
      traceId: isSet(object.traceId)
        ? globalThis.String(object.traceId)
        : isSet(object.trace_id)
        ? globalThis.String(object.trace_id)
        : "",
      checkedAt: isSet(object.checkedAt)
        ? globalThis.String(object.checkedAt)
        : isSet(object.checked_at)
        ? globalThis.String(object.checked_at)
        : "",
    };
  },

  toJSON(message: HealthResponse): unknown {
    const obj: any = {};
    if (message.service !== "") {
      obj.service = message.service;
    }
    if (message.status !== "") {
      obj.status = message.status;
    }
    if (message.version !== "") {
      obj.version = message.version;
    }
    if (message.requestId !== "") {
      obj.requestId = message.requestId;
    }
    if (message.traceId !== "") {
      obj.traceId = message.traceId;
    }
    if (message.checkedAt !== "") {
      obj.checkedAt = message.checkedAt;
    }
    return obj;
  },

  create(base?: DeepPartial<HealthResponse>): HealthResponse {
    return HealthResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<HealthResponse>): HealthResponse {
    const message = createBaseHealthResponse();
    message.service = object.service ?? "";
    message.status = object.status ?? "";
    message.version = object.version ?? "";
    message.requestId = object.requestId ?? "";
    message.traceId = object.traceId ?? "";
    message.checkedAt = object.checkedAt ?? "";
    return message;
  },
};

function createBaseIdentityUser(): IdentityUser {
  return { id: "", email: "", displayName: "", role: 0 };
}

export const IdentityUser: MessageFns<IdentityUser> = {
  encode(message: IdentityUser, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.email !== "") {
      writer.uint32(18).string(message.email);
    }
    if (message.displayName !== "") {
      writer.uint32(26).string(message.displayName);
    }
    if (message.role !== 0) {
      writer.uint32(32).int32(message.role);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): IdentityUser {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIdentityUser();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.email = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.displayName = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.role = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IdentityUser {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      email: isSet(object.email) ? globalThis.String(object.email) : "",
      displayName: isSet(object.displayName)
        ? globalThis.String(object.displayName)
        : isSet(object.display_name)
        ? globalThis.String(object.display_name)
        : "",
      role: isSet(object.role) ? identityRoleFromJSON(object.role) : 0,
    };
  },

  toJSON(message: IdentityUser): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.email !== "") {
      obj.email = message.email;
    }
    if (message.displayName !== "") {
      obj.displayName = message.displayName;
    }
    if (message.role !== 0) {
      obj.role = identityRoleToJSON(message.role);
    }
    return obj;
  },

  create(base?: DeepPartial<IdentityUser>): IdentityUser {
    return IdentityUser.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<IdentityUser>): IdentityUser {
    const message = createBaseIdentityUser();
    message.id = object.id ?? "";
    message.email = object.email ?? "";
    message.displayName = object.displayName ?? "";
    message.role = object.role ?? 0;
    return message;
  },
};

function createBaseAuthSession(): AuthSession {
  return { accessToken: "", refreshToken: "", accessExpiresAt: "", refreshExpiresAt: "", user: undefined };
}

export const AuthSession: MessageFns<AuthSession> = {
  encode(message: AuthSession, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.accessToken !== "") {
      writer.uint32(10).string(message.accessToken);
    }
    if (message.refreshToken !== "") {
      writer.uint32(18).string(message.refreshToken);
    }
    if (message.accessExpiresAt !== "") {
      writer.uint32(26).string(message.accessExpiresAt);
    }
    if (message.refreshExpiresAt !== "") {
      writer.uint32(34).string(message.refreshExpiresAt);
    }
    if (message.user !== undefined) {
      IdentityUser.encode(message.user, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AuthSession {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAuthSession();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.accessToken = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.refreshToken = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.accessExpiresAt = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.refreshExpiresAt = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.user = IdentityUser.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AuthSession {
    return {
      accessToken: isSet(object.accessToken)
        ? globalThis.String(object.accessToken)
        : isSet(object.access_token)
        ? globalThis.String(object.access_token)
        : "",
      refreshToken: isSet(object.refreshToken)
        ? globalThis.String(object.refreshToken)
        : isSet(object.refresh_token)
        ? globalThis.String(object.refresh_token)
        : "",
      accessExpiresAt: isSet(object.accessExpiresAt)
        ? globalThis.String(object.accessExpiresAt)
        : isSet(object.access_expires_at)
        ? globalThis.String(object.access_expires_at)
        : "",
      refreshExpiresAt: isSet(object.refreshExpiresAt)
        ? globalThis.String(object.refreshExpiresAt)
        : isSet(object.refresh_expires_at)
        ? globalThis.String(object.refresh_expires_at)
        : "",
      user: isSet(object.user) ? IdentityUser.fromJSON(object.user) : undefined,
    };
  },

  toJSON(message: AuthSession): unknown {
    const obj: any = {};
    if (message.accessToken !== "") {
      obj.accessToken = message.accessToken;
    }
    if (message.refreshToken !== "") {
      obj.refreshToken = message.refreshToken;
    }
    if (message.accessExpiresAt !== "") {
      obj.accessExpiresAt = message.accessExpiresAt;
    }
    if (message.refreshExpiresAt !== "") {
      obj.refreshExpiresAt = message.refreshExpiresAt;
    }
    if (message.user !== undefined) {
      obj.user = IdentityUser.toJSON(message.user);
    }
    return obj;
  },

  create(base?: DeepPartial<AuthSession>): AuthSession {
    return AuthSession.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AuthSession>): AuthSession {
    const message = createBaseAuthSession();
    message.accessToken = object.accessToken ?? "";
    message.refreshToken = object.refreshToken ?? "";
    message.accessExpiresAt = object.accessExpiresAt ?? "";
    message.refreshExpiresAt = object.refreshExpiresAt ?? "";
    message.user = (object.user !== undefined && object.user !== null)
      ? IdentityUser.fromPartial(object.user)
      : undefined;
    return message;
  },
};

function createBaseLoginRequest(): LoginRequest {
  return { email: "", password: "", requestId: "" };
}

export const LoginRequest: MessageFns<LoginRequest> = {
  encode(message: LoginRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.email !== "") {
      writer.uint32(10).string(message.email);
    }
    if (message.password !== "") {
      writer.uint32(18).string(message.password);
    }
    if (message.requestId !== "") {
      writer.uint32(26).string(message.requestId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LoginRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLoginRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.email = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.password = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.requestId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LoginRequest {
    return {
      email: isSet(object.email) ? globalThis.String(object.email) : "",
      password: isSet(object.password) ? globalThis.String(object.password) : "",
      requestId: isSet(object.requestId)
        ? globalThis.String(object.requestId)
        : isSet(object.request_id)
        ? globalThis.String(object.request_id)
        : "",
    };
  },

  toJSON(message: LoginRequest): unknown {
    const obj: any = {};
    if (message.email !== "") {
      obj.email = message.email;
    }
    if (message.password !== "") {
      obj.password = message.password;
    }
    if (message.requestId !== "") {
      obj.requestId = message.requestId;
    }
    return obj;
  },

  create(base?: DeepPartial<LoginRequest>): LoginRequest {
    return LoginRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<LoginRequest>): LoginRequest {
    const message = createBaseLoginRequest();
    message.email = object.email ?? "";
    message.password = object.password ?? "";
    message.requestId = object.requestId ?? "";
    return message;
  },
};

function createBaseRefreshRequest(): RefreshRequest {
  return { refreshToken: "", requestId: "" };
}

export const RefreshRequest: MessageFns<RefreshRequest> = {
  encode(message: RefreshRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.refreshToken !== "") {
      writer.uint32(10).string(message.refreshToken);
    }
    if (message.requestId !== "") {
      writer.uint32(18).string(message.requestId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RefreshRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRefreshRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.refreshToken = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.requestId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RefreshRequest {
    return {
      refreshToken: isSet(object.refreshToken)
        ? globalThis.String(object.refreshToken)
        : isSet(object.refresh_token)
        ? globalThis.String(object.refresh_token)
        : "",
      requestId: isSet(object.requestId)
        ? globalThis.String(object.requestId)
        : isSet(object.request_id)
        ? globalThis.String(object.request_id)
        : "",
    };
  },

  toJSON(message: RefreshRequest): unknown {
    const obj: any = {};
    if (message.refreshToken !== "") {
      obj.refreshToken = message.refreshToken;
    }
    if (message.requestId !== "") {
      obj.requestId = message.requestId;
    }
    return obj;
  },

  create(base?: DeepPartial<RefreshRequest>): RefreshRequest {
    return RefreshRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RefreshRequest>): RefreshRequest {
    const message = createBaseRefreshRequest();
    message.refreshToken = object.refreshToken ?? "";
    message.requestId = object.requestId ?? "";
    return message;
  },
};

function createBaseLoginResponse(): LoginResponse {
  return { session: undefined, requestId: "" };
}

export const LoginResponse: MessageFns<LoginResponse> = {
  encode(message: LoginResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.session !== undefined) {
      AuthSession.encode(message.session, writer.uint32(10).fork()).join();
    }
    if (message.requestId !== "") {
      writer.uint32(18).string(message.requestId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LoginResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLoginResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.session = AuthSession.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.requestId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LoginResponse {
    return {
      session: isSet(object.session) ? AuthSession.fromJSON(object.session) : undefined,
      requestId: isSet(object.requestId)
        ? globalThis.String(object.requestId)
        : isSet(object.request_id)
        ? globalThis.String(object.request_id)
        : "",
    };
  },

  toJSON(message: LoginResponse): unknown {
    const obj: any = {};
    if (message.session !== undefined) {
      obj.session = AuthSession.toJSON(message.session);
    }
    if (message.requestId !== "") {
      obj.requestId = message.requestId;
    }
    return obj;
  },

  create(base?: DeepPartial<LoginResponse>): LoginResponse {
    return LoginResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<LoginResponse>): LoginResponse {
    const message = createBaseLoginResponse();
    message.session = (object.session !== undefined && object.session !== null)
      ? AuthSession.fromPartial(object.session)
      : undefined;
    message.requestId = object.requestId ?? "";
    return message;
  },
};

function createBaseRefreshResponse(): RefreshResponse {
  return { session: undefined, requestId: "" };
}

export const RefreshResponse: MessageFns<RefreshResponse> = {
  encode(message: RefreshResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.session !== undefined) {
      AuthSession.encode(message.session, writer.uint32(10).fork()).join();
    }
    if (message.requestId !== "") {
      writer.uint32(18).string(message.requestId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RefreshResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRefreshResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.session = AuthSession.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.requestId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RefreshResponse {
    return {
      session: isSet(object.session) ? AuthSession.fromJSON(object.session) : undefined,
      requestId: isSet(object.requestId)
        ? globalThis.String(object.requestId)
        : isSet(object.request_id)
        ? globalThis.String(object.request_id)
        : "",
    };
  },

  toJSON(message: RefreshResponse): unknown {
    const obj: any = {};
    if (message.session !== undefined) {
      obj.session = AuthSession.toJSON(message.session);
    }
    if (message.requestId !== "") {
      obj.requestId = message.requestId;
    }
    return obj;
  },

  create(base?: DeepPartial<RefreshResponse>): RefreshResponse {
    return RefreshResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RefreshResponse>): RefreshResponse {
    const message = createBaseRefreshResponse();
    message.session = (object.session !== undefined && object.session !== null)
      ? AuthSession.fromPartial(object.session)
      : undefined;
    message.requestId = object.requestId ?? "";
    return message;
  },
};

function createBaseLogoutRequest(): LogoutRequest {
  return { refreshToken: "", requestId: "" };
}

export const LogoutRequest: MessageFns<LogoutRequest> = {
  encode(message: LogoutRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.refreshToken !== "") {
      writer.uint32(10).string(message.refreshToken);
    }
    if (message.requestId !== "") {
      writer.uint32(18).string(message.requestId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LogoutRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLogoutRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.refreshToken = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.requestId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LogoutRequest {
    return {
      refreshToken: isSet(object.refreshToken)
        ? globalThis.String(object.refreshToken)
        : isSet(object.refresh_token)
        ? globalThis.String(object.refresh_token)
        : "",
      requestId: isSet(object.requestId)
        ? globalThis.String(object.requestId)
        : isSet(object.request_id)
        ? globalThis.String(object.request_id)
        : "",
    };
  },

  toJSON(message: LogoutRequest): unknown {
    const obj: any = {};
    if (message.refreshToken !== "") {
      obj.refreshToken = message.refreshToken;
    }
    if (message.requestId !== "") {
      obj.requestId = message.requestId;
    }
    return obj;
  },

  create(base?: DeepPartial<LogoutRequest>): LogoutRequest {
    return LogoutRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<LogoutRequest>): LogoutRequest {
    const message = createBaseLogoutRequest();
    message.refreshToken = object.refreshToken ?? "";
    message.requestId = object.requestId ?? "";
    return message;
  },
};

function createBaseLogoutResponse(): LogoutResponse {
  return { revoked: false, requestId: "" };
}

export const LogoutResponse: MessageFns<LogoutResponse> = {
  encode(message: LogoutResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.revoked !== false) {
      writer.uint32(8).bool(message.revoked);
    }
    if (message.requestId !== "") {
      writer.uint32(18).string(message.requestId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LogoutResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLogoutResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.revoked = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.requestId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LogoutResponse {
    return {
      revoked: isSet(object.revoked) ? globalThis.Boolean(object.revoked) : false,
      requestId: isSet(object.requestId)
        ? globalThis.String(object.requestId)
        : isSet(object.request_id)
        ? globalThis.String(object.request_id)
        : "",
    };
  },

  toJSON(message: LogoutResponse): unknown {
    const obj: any = {};
    if (message.revoked !== false) {
      obj.revoked = message.revoked;
    }
    if (message.requestId !== "") {
      obj.requestId = message.requestId;
    }
    return obj;
  },

  create(base?: DeepPartial<LogoutResponse>): LogoutResponse {
    return LogoutResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<LogoutResponse>): LogoutResponse {
    const message = createBaseLogoutResponse();
    message.revoked = object.revoked ?? false;
    message.requestId = object.requestId ?? "";
    return message;
  },
};

function createBaseValidateAccessTokenRequest(): ValidateAccessTokenRequest {
  return { accessToken: "", requestId: "" };
}

export const ValidateAccessTokenRequest: MessageFns<ValidateAccessTokenRequest> = {
  encode(message: ValidateAccessTokenRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.accessToken !== "") {
      writer.uint32(10).string(message.accessToken);
    }
    if (message.requestId !== "") {
      writer.uint32(18).string(message.requestId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ValidateAccessTokenRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseValidateAccessTokenRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.accessToken = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.requestId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ValidateAccessTokenRequest {
    return {
      accessToken: isSet(object.accessToken)
        ? globalThis.String(object.accessToken)
        : isSet(object.access_token)
        ? globalThis.String(object.access_token)
        : "",
      requestId: isSet(object.requestId)
        ? globalThis.String(object.requestId)
        : isSet(object.request_id)
        ? globalThis.String(object.request_id)
        : "",
    };
  },

  toJSON(message: ValidateAccessTokenRequest): unknown {
    const obj: any = {};
    if (message.accessToken !== "") {
      obj.accessToken = message.accessToken;
    }
    if (message.requestId !== "") {
      obj.requestId = message.requestId;
    }
    return obj;
  },

  create(base?: DeepPartial<ValidateAccessTokenRequest>): ValidateAccessTokenRequest {
    return ValidateAccessTokenRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ValidateAccessTokenRequest>): ValidateAccessTokenRequest {
    const message = createBaseValidateAccessTokenRequest();
    message.accessToken = object.accessToken ?? "";
    message.requestId = object.requestId ?? "";
    return message;
  },
};

function createBaseAuthenticatedActor(): AuthenticatedActor {
  return { id: "", email: "", displayName: "", role: 0, tokenId: "", expiresAt: "" };
}

export const AuthenticatedActor: MessageFns<AuthenticatedActor> = {
  encode(message: AuthenticatedActor, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.email !== "") {
      writer.uint32(18).string(message.email);
    }
    if (message.displayName !== "") {
      writer.uint32(26).string(message.displayName);
    }
    if (message.role !== 0) {
      writer.uint32(32).int32(message.role);
    }
    if (message.tokenId !== "") {
      writer.uint32(42).string(message.tokenId);
    }
    if (message.expiresAt !== "") {
      writer.uint32(50).string(message.expiresAt);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AuthenticatedActor {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAuthenticatedActor();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.email = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.displayName = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.role = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.tokenId = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.expiresAt = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AuthenticatedActor {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      email: isSet(object.email) ? globalThis.String(object.email) : "",
      displayName: isSet(object.displayName)
        ? globalThis.String(object.displayName)
        : isSet(object.display_name)
        ? globalThis.String(object.display_name)
        : "",
      role: isSet(object.role) ? identityRoleFromJSON(object.role) : 0,
      tokenId: isSet(object.tokenId)
        ? globalThis.String(object.tokenId)
        : isSet(object.token_id)
        ? globalThis.String(object.token_id)
        : "",
      expiresAt: isSet(object.expiresAt)
        ? globalThis.String(object.expiresAt)
        : isSet(object.expires_at)
        ? globalThis.String(object.expires_at)
        : "",
    };
  },

  toJSON(message: AuthenticatedActor): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.email !== "") {
      obj.email = message.email;
    }
    if (message.displayName !== "") {
      obj.displayName = message.displayName;
    }
    if (message.role !== 0) {
      obj.role = identityRoleToJSON(message.role);
    }
    if (message.tokenId !== "") {
      obj.tokenId = message.tokenId;
    }
    if (message.expiresAt !== "") {
      obj.expiresAt = message.expiresAt;
    }
    return obj;
  },

  create(base?: DeepPartial<AuthenticatedActor>): AuthenticatedActor {
    return AuthenticatedActor.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AuthenticatedActor>): AuthenticatedActor {
    const message = createBaseAuthenticatedActor();
    message.id = object.id ?? "";
    message.email = object.email ?? "";
    message.displayName = object.displayName ?? "";
    message.role = object.role ?? 0;
    message.tokenId = object.tokenId ?? "";
    message.expiresAt = object.expiresAt ?? "";
    return message;
  },
};

function createBaseValidateAccessTokenResponse(): ValidateAccessTokenResponse {
  return { actor: undefined, requestId: "" };
}

export const ValidateAccessTokenResponse: MessageFns<ValidateAccessTokenResponse> = {
  encode(message: ValidateAccessTokenResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.actor !== undefined) {
      AuthenticatedActor.encode(message.actor, writer.uint32(10).fork()).join();
    }
    if (message.requestId !== "") {
      writer.uint32(18).string(message.requestId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ValidateAccessTokenResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseValidateAccessTokenResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.actor = AuthenticatedActor.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.requestId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ValidateAccessTokenResponse {
    return {
      actor: isSet(object.actor) ? AuthenticatedActor.fromJSON(object.actor) : undefined,
      requestId: isSet(object.requestId)
        ? globalThis.String(object.requestId)
        : isSet(object.request_id)
        ? globalThis.String(object.request_id)
        : "",
    };
  },

  toJSON(message: ValidateAccessTokenResponse): unknown {
    const obj: any = {};
    if (message.actor !== undefined) {
      obj.actor = AuthenticatedActor.toJSON(message.actor);
    }
    if (message.requestId !== "") {
      obj.requestId = message.requestId;
    }
    return obj;
  },

  create(base?: DeepPartial<ValidateAccessTokenResponse>): ValidateAccessTokenResponse {
    return ValidateAccessTokenResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ValidateAccessTokenResponse>): ValidateAccessTokenResponse {
    const message = createBaseValidateAccessTokenResponse();
    message.actor = (object.actor !== undefined && object.actor !== null)
      ? AuthenticatedActor.fromPartial(object.actor)
      : undefined;
    message.requestId = object.requestId ?? "";
    return message;
  },
};

function createBasePassengerProfile(): PassengerProfile {
  return { id: "", label: "", fullName: "", phone: undefined, createdAt: "", updatedAt: "" };
}

export const PassengerProfile: MessageFns<PassengerProfile> = {
  encode(message: PassengerProfile, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.label !== "") {
      writer.uint32(18).string(message.label);
    }
    if (message.fullName !== "") {
      writer.uint32(26).string(message.fullName);
    }
    if (message.phone !== undefined) {
      writer.uint32(34).string(message.phone);
    }
    if (message.createdAt !== "") {
      writer.uint32(42).string(message.createdAt);
    }
    if (message.updatedAt !== "") {
      writer.uint32(50).string(message.updatedAt);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PassengerProfile {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePassengerProfile();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.label = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.fullName = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.phone = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.createdAt = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.updatedAt = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PassengerProfile {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      label: isSet(object.label) ? globalThis.String(object.label) : "",
      fullName: isSet(object.fullName)
        ? globalThis.String(object.fullName)
        : isSet(object.full_name)
        ? globalThis.String(object.full_name)
        : "",
      phone: isSet(object.phone) ? globalThis.String(object.phone) : undefined,
      createdAt: isSet(object.createdAt)
        ? globalThis.String(object.createdAt)
        : isSet(object.created_at)
        ? globalThis.String(object.created_at)
        : "",
      updatedAt: isSet(object.updatedAt)
        ? globalThis.String(object.updatedAt)
        : isSet(object.updated_at)
        ? globalThis.String(object.updated_at)
        : "",
    };
  },

  toJSON(message: PassengerProfile): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.label !== "") {
      obj.label = message.label;
    }
    if (message.fullName !== "") {
      obj.fullName = message.fullName;
    }
    if (message.phone !== undefined) {
      obj.phone = message.phone;
    }
    if (message.createdAt !== "") {
      obj.createdAt = message.createdAt;
    }
    if (message.updatedAt !== "") {
      obj.updatedAt = message.updatedAt;
    }
    return obj;
  },

  create(base?: DeepPartial<PassengerProfile>): PassengerProfile {
    return PassengerProfile.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PassengerProfile>): PassengerProfile {
    const message = createBasePassengerProfile();
    message.id = object.id ?? "";
    message.label = object.label ?? "";
    message.fullName = object.fullName ?? "";
    message.phone = object.phone ?? undefined;
    message.createdAt = object.createdAt ?? "";
    message.updatedAt = object.updatedAt ?? "";
    return message;
  },
};

function createBasePassengerProfileInput(): PassengerProfileInput {
  return { label: "", fullName: "", phone: undefined };
}

export const PassengerProfileInput: MessageFns<PassengerProfileInput> = {
  encode(message: PassengerProfileInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.label !== "") {
      writer.uint32(10).string(message.label);
    }
    if (message.fullName !== "") {
      writer.uint32(18).string(message.fullName);
    }
    if (message.phone !== undefined) {
      writer.uint32(26).string(message.phone);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PassengerProfileInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePassengerProfileInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.label = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.fullName = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.phone = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PassengerProfileInput {
    return {
      label: isSet(object.label) ? globalThis.String(object.label) : "",
      fullName: isSet(object.fullName)
        ? globalThis.String(object.fullName)
        : isSet(object.full_name)
        ? globalThis.String(object.full_name)
        : "",
      phone: isSet(object.phone) ? globalThis.String(object.phone) : undefined,
    };
  },

  toJSON(message: PassengerProfileInput): unknown {
    const obj: any = {};
    if (message.label !== "") {
      obj.label = message.label;
    }
    if (message.fullName !== "") {
      obj.fullName = message.fullName;
    }
    if (message.phone !== undefined) {
      obj.phone = message.phone;
    }
    return obj;
  },

  create(base?: DeepPartial<PassengerProfileInput>): PassengerProfileInput {
    return PassengerProfileInput.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PassengerProfileInput>): PassengerProfileInput {
    const message = createBasePassengerProfileInput();
    message.label = object.label ?? "";
    message.fullName = object.fullName ?? "";
    message.phone = object.phone ?? undefined;
    return message;
  },
};

function createBaseListPassengerProfilesRequest(): ListPassengerProfilesRequest {
  return { requestId: "" };
}

export const ListPassengerProfilesRequest: MessageFns<ListPassengerProfilesRequest> = {
  encode(message: ListPassengerProfilesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.requestId !== "") {
      writer.uint32(10).string(message.requestId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListPassengerProfilesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListPassengerProfilesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.requestId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListPassengerProfilesRequest {
    return {
      requestId: isSet(object.requestId)
        ? globalThis.String(object.requestId)
        : isSet(object.request_id)
        ? globalThis.String(object.request_id)
        : "",
    };
  },

  toJSON(message: ListPassengerProfilesRequest): unknown {
    const obj: any = {};
    if (message.requestId !== "") {
      obj.requestId = message.requestId;
    }
    return obj;
  },

  create(base?: DeepPartial<ListPassengerProfilesRequest>): ListPassengerProfilesRequest {
    return ListPassengerProfilesRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListPassengerProfilesRequest>): ListPassengerProfilesRequest {
    const message = createBaseListPassengerProfilesRequest();
    message.requestId = object.requestId ?? "";
    return message;
  },
};

function createBaseListPassengerProfilesResponse(): ListPassengerProfilesResponse {
  return { profiles: [], requestId: "" };
}

export const ListPassengerProfilesResponse: MessageFns<ListPassengerProfilesResponse> = {
  encode(message: ListPassengerProfilesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.profiles) {
      PassengerProfile.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.requestId !== "") {
      writer.uint32(18).string(message.requestId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListPassengerProfilesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListPassengerProfilesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.profiles.push(PassengerProfile.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.requestId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListPassengerProfilesResponse {
    return {
      profiles: globalThis.Array.isArray(object?.profiles)
        ? object.profiles.map((e: any) => PassengerProfile.fromJSON(e))
        : [],
      requestId: isSet(object.requestId)
        ? globalThis.String(object.requestId)
        : isSet(object.request_id)
        ? globalThis.String(object.request_id)
        : "",
    };
  },

  toJSON(message: ListPassengerProfilesResponse): unknown {
    const obj: any = {};
    if (message.profiles?.length) {
      obj.profiles = message.profiles.map((e) => PassengerProfile.toJSON(e));
    }
    if (message.requestId !== "") {
      obj.requestId = message.requestId;
    }
    return obj;
  },

  create(base?: DeepPartial<ListPassengerProfilesResponse>): ListPassengerProfilesResponse {
    return ListPassengerProfilesResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListPassengerProfilesResponse>): ListPassengerProfilesResponse {
    const message = createBaseListPassengerProfilesResponse();
    message.profiles = object.profiles?.map((e) => PassengerProfile.fromPartial(e)) || [];
    message.requestId = object.requestId ?? "";
    return message;
  },
};

function createBaseCreatePassengerProfileRequest(): CreatePassengerProfileRequest {
  return { input: undefined, requestId: "" };
}

export const CreatePassengerProfileRequest: MessageFns<CreatePassengerProfileRequest> = {
  encode(message: CreatePassengerProfileRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.input !== undefined) {
      PassengerProfileInput.encode(message.input, writer.uint32(10).fork()).join();
    }
    if (message.requestId !== "") {
      writer.uint32(18).string(message.requestId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreatePassengerProfileRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreatePassengerProfileRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.input = PassengerProfileInput.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.requestId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreatePassengerProfileRequest {
    return {
      input: isSet(object.input) ? PassengerProfileInput.fromJSON(object.input) : undefined,
      requestId: isSet(object.requestId)
        ? globalThis.String(object.requestId)
        : isSet(object.request_id)
        ? globalThis.String(object.request_id)
        : "",
    };
  },

  toJSON(message: CreatePassengerProfileRequest): unknown {
    const obj: any = {};
    if (message.input !== undefined) {
      obj.input = PassengerProfileInput.toJSON(message.input);
    }
    if (message.requestId !== "") {
      obj.requestId = message.requestId;
    }
    return obj;
  },

  create(base?: DeepPartial<CreatePassengerProfileRequest>): CreatePassengerProfileRequest {
    return CreatePassengerProfileRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CreatePassengerProfileRequest>): CreatePassengerProfileRequest {
    const message = createBaseCreatePassengerProfileRequest();
    message.input = (object.input !== undefined && object.input !== null)
      ? PassengerProfileInput.fromPartial(object.input)
      : undefined;
    message.requestId = object.requestId ?? "";
    return message;
  },
};

function createBaseUpdatePassengerProfileRequest(): UpdatePassengerProfileRequest {
  return { profileId: "", input: undefined, requestId: "" };
}

export const UpdatePassengerProfileRequest: MessageFns<UpdatePassengerProfileRequest> = {
  encode(message: UpdatePassengerProfileRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.profileId !== "") {
      writer.uint32(10).string(message.profileId);
    }
    if (message.input !== undefined) {
      PassengerProfileInput.encode(message.input, writer.uint32(18).fork()).join();
    }
    if (message.requestId !== "") {
      writer.uint32(26).string(message.requestId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdatePassengerProfileRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdatePassengerProfileRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.profileId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.input = PassengerProfileInput.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.requestId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdatePassengerProfileRequest {
    return {
      profileId: isSet(object.profileId)
        ? globalThis.String(object.profileId)
        : isSet(object.profile_id)
        ? globalThis.String(object.profile_id)
        : "",
      input: isSet(object.input) ? PassengerProfileInput.fromJSON(object.input) : undefined,
      requestId: isSet(object.requestId)
        ? globalThis.String(object.requestId)
        : isSet(object.request_id)
        ? globalThis.String(object.request_id)
        : "",
    };
  },

  toJSON(message: UpdatePassengerProfileRequest): unknown {
    const obj: any = {};
    if (message.profileId !== "") {
      obj.profileId = message.profileId;
    }
    if (message.input !== undefined) {
      obj.input = PassengerProfileInput.toJSON(message.input);
    }
    if (message.requestId !== "") {
      obj.requestId = message.requestId;
    }
    return obj;
  },

  create(base?: DeepPartial<UpdatePassengerProfileRequest>): UpdatePassengerProfileRequest {
    return UpdatePassengerProfileRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UpdatePassengerProfileRequest>): UpdatePassengerProfileRequest {
    const message = createBaseUpdatePassengerProfileRequest();
    message.profileId = object.profileId ?? "";
    message.input = (object.input !== undefined && object.input !== null)
      ? PassengerProfileInput.fromPartial(object.input)
      : undefined;
    message.requestId = object.requestId ?? "";
    return message;
  },
};

function createBaseCreatePassengerProfileResponse(): CreatePassengerProfileResponse {
  return { profile: undefined, requestId: "" };
}

export const CreatePassengerProfileResponse: MessageFns<CreatePassengerProfileResponse> = {
  encode(message: CreatePassengerProfileResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.profile !== undefined) {
      PassengerProfile.encode(message.profile, writer.uint32(10).fork()).join();
    }
    if (message.requestId !== "") {
      writer.uint32(18).string(message.requestId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreatePassengerProfileResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreatePassengerProfileResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.profile = PassengerProfile.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.requestId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreatePassengerProfileResponse {
    return {
      profile: isSet(object.profile) ? PassengerProfile.fromJSON(object.profile) : undefined,
      requestId: isSet(object.requestId)
        ? globalThis.String(object.requestId)
        : isSet(object.request_id)
        ? globalThis.String(object.request_id)
        : "",
    };
  },

  toJSON(message: CreatePassengerProfileResponse): unknown {
    const obj: any = {};
    if (message.profile !== undefined) {
      obj.profile = PassengerProfile.toJSON(message.profile);
    }
    if (message.requestId !== "") {
      obj.requestId = message.requestId;
    }
    return obj;
  },

  create(base?: DeepPartial<CreatePassengerProfileResponse>): CreatePassengerProfileResponse {
    return CreatePassengerProfileResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CreatePassengerProfileResponse>): CreatePassengerProfileResponse {
    const message = createBaseCreatePassengerProfileResponse();
    message.profile = (object.profile !== undefined && object.profile !== null)
      ? PassengerProfile.fromPartial(object.profile)
      : undefined;
    message.requestId = object.requestId ?? "";
    return message;
  },
};

function createBaseUpdatePassengerProfileResponse(): UpdatePassengerProfileResponse {
  return { profile: undefined, requestId: "" };
}

export const UpdatePassengerProfileResponse: MessageFns<UpdatePassengerProfileResponse> = {
  encode(message: UpdatePassengerProfileResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.profile !== undefined) {
      PassengerProfile.encode(message.profile, writer.uint32(10).fork()).join();
    }
    if (message.requestId !== "") {
      writer.uint32(18).string(message.requestId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdatePassengerProfileResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdatePassengerProfileResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.profile = PassengerProfile.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.requestId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdatePassengerProfileResponse {
    return {
      profile: isSet(object.profile) ? PassengerProfile.fromJSON(object.profile) : undefined,
      requestId: isSet(object.requestId)
        ? globalThis.String(object.requestId)
        : isSet(object.request_id)
        ? globalThis.String(object.request_id)
        : "",
    };
  },

  toJSON(message: UpdatePassengerProfileResponse): unknown {
    const obj: any = {};
    if (message.profile !== undefined) {
      obj.profile = PassengerProfile.toJSON(message.profile);
    }
    if (message.requestId !== "") {
      obj.requestId = message.requestId;
    }
    return obj;
  },

  create(base?: DeepPartial<UpdatePassengerProfileResponse>): UpdatePassengerProfileResponse {
    return UpdatePassengerProfileResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UpdatePassengerProfileResponse>): UpdatePassengerProfileResponse {
    const message = createBaseUpdatePassengerProfileResponse();
    message.profile = (object.profile !== undefined && object.profile !== null)
      ? PassengerProfile.fromPartial(object.profile)
      : undefined;
    message.requestId = object.requestId ?? "";
    return message;
  },
};

function createBaseDeletePassengerProfileRequest(): DeletePassengerProfileRequest {
  return { profileId: "", requestId: "" };
}

export const DeletePassengerProfileRequest: MessageFns<DeletePassengerProfileRequest> = {
  encode(message: DeletePassengerProfileRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.profileId !== "") {
      writer.uint32(10).string(message.profileId);
    }
    if (message.requestId !== "") {
      writer.uint32(18).string(message.requestId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeletePassengerProfileRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeletePassengerProfileRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.profileId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.requestId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeletePassengerProfileRequest {
    return {
      profileId: isSet(object.profileId)
        ? globalThis.String(object.profileId)
        : isSet(object.profile_id)
        ? globalThis.String(object.profile_id)
        : "",
      requestId: isSet(object.requestId)
        ? globalThis.String(object.requestId)
        : isSet(object.request_id)
        ? globalThis.String(object.request_id)
        : "",
    };
  },

  toJSON(message: DeletePassengerProfileRequest): unknown {
    const obj: any = {};
    if (message.profileId !== "") {
      obj.profileId = message.profileId;
    }
    if (message.requestId !== "") {
      obj.requestId = message.requestId;
    }
    return obj;
  },

  create(base?: DeepPartial<DeletePassengerProfileRequest>): DeletePassengerProfileRequest {
    return DeletePassengerProfileRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<DeletePassengerProfileRequest>): DeletePassengerProfileRequest {
    const message = createBaseDeletePassengerProfileRequest();
    message.profileId = object.profileId ?? "";
    message.requestId = object.requestId ?? "";
    return message;
  },
};

function createBaseDeletePassengerProfileResponse(): DeletePassengerProfileResponse {
  return { profileId: "", deleted: false, requestId: "" };
}

export const DeletePassengerProfileResponse: MessageFns<DeletePassengerProfileResponse> = {
  encode(message: DeletePassengerProfileResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.profileId !== "") {
      writer.uint32(10).string(message.profileId);
    }
    if (message.deleted !== false) {
      writer.uint32(16).bool(message.deleted);
    }
    if (message.requestId !== "") {
      writer.uint32(26).string(message.requestId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeletePassengerProfileResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeletePassengerProfileResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.profileId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.deleted = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.requestId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeletePassengerProfileResponse {
    return {
      profileId: isSet(object.profileId)
        ? globalThis.String(object.profileId)
        : isSet(object.profile_id)
        ? globalThis.String(object.profile_id)
        : "",
      deleted: isSet(object.deleted) ? globalThis.Boolean(object.deleted) : false,
      requestId: isSet(object.requestId)
        ? globalThis.String(object.requestId)
        : isSet(object.request_id)
        ? globalThis.String(object.request_id)
        : "",
    };
  },

  toJSON(message: DeletePassengerProfileResponse): unknown {
    const obj: any = {};
    if (message.profileId !== "") {
      obj.profileId = message.profileId;
    }
    if (message.deleted !== false) {
      obj.deleted = message.deleted;
    }
    if (message.requestId !== "") {
      obj.requestId = message.requestId;
    }
    return obj;
  },

  create(base?: DeepPartial<DeletePassengerProfileResponse>): DeletePassengerProfileResponse {
    return DeletePassengerProfileResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<DeletePassengerProfileResponse>): DeletePassengerProfileResponse {
    const message = createBaseDeletePassengerProfileResponse();
    message.profileId = object.profileId ?? "";
    message.deleted = object.deleted ?? false;
    message.requestId = object.requestId ?? "";
    return message;
  },
};

export type IdentityServiceService = typeof IdentityServiceService;
export const IdentityServiceService = {
  health: {
    path: "/bus.identity.v1.IdentityService/Health" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: HealthRequest): Buffer => Buffer.from(HealthRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): HealthRequest => HealthRequest.decode(value),
    responseSerialize: (value: HealthResponse): Buffer => Buffer.from(HealthResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): HealthResponse => HealthResponse.decode(value),
  },
  login: {
    path: "/bus.identity.v1.IdentityService/Login" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: LoginRequest): Buffer => Buffer.from(LoginRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): LoginRequest => LoginRequest.decode(value),
    responseSerialize: (value: LoginResponse): Buffer => Buffer.from(LoginResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): LoginResponse => LoginResponse.decode(value),
  },
  refresh: {
    path: "/bus.identity.v1.IdentityService/Refresh" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: RefreshRequest): Buffer => Buffer.from(RefreshRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): RefreshRequest => RefreshRequest.decode(value),
    responseSerialize: (value: RefreshResponse): Buffer => Buffer.from(RefreshResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): RefreshResponse => RefreshResponse.decode(value),
  },
  logout: {
    path: "/bus.identity.v1.IdentityService/Logout" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: LogoutRequest): Buffer => Buffer.from(LogoutRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): LogoutRequest => LogoutRequest.decode(value),
    responseSerialize: (value: LogoutResponse): Buffer => Buffer.from(LogoutResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): LogoutResponse => LogoutResponse.decode(value),
  },
  validateAccessToken: {
    path: "/bus.identity.v1.IdentityService/ValidateAccessToken" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ValidateAccessTokenRequest): Buffer =>
      Buffer.from(ValidateAccessTokenRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ValidateAccessTokenRequest => ValidateAccessTokenRequest.decode(value),
    responseSerialize: (value: ValidateAccessTokenResponse): Buffer =>
      Buffer.from(ValidateAccessTokenResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ValidateAccessTokenResponse => ValidateAccessTokenResponse.decode(value),
  },
  listPassengerProfiles: {
    path: "/bus.identity.v1.IdentityService/ListPassengerProfiles" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ListPassengerProfilesRequest): Buffer =>
      Buffer.from(ListPassengerProfilesRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ListPassengerProfilesRequest => ListPassengerProfilesRequest.decode(value),
    responseSerialize: (value: ListPassengerProfilesResponse): Buffer =>
      Buffer.from(ListPassengerProfilesResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ListPassengerProfilesResponse => ListPassengerProfilesResponse.decode(value),
  },
  createPassengerProfile: {
    path: "/bus.identity.v1.IdentityService/CreatePassengerProfile" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: CreatePassengerProfileRequest): Buffer =>
      Buffer.from(CreatePassengerProfileRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): CreatePassengerProfileRequest => CreatePassengerProfileRequest.decode(value),
    responseSerialize: (value: CreatePassengerProfileResponse): Buffer =>
      Buffer.from(CreatePassengerProfileResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): CreatePassengerProfileResponse =>
      CreatePassengerProfileResponse.decode(value),
  },
  updatePassengerProfile: {
    path: "/bus.identity.v1.IdentityService/UpdatePassengerProfile" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: UpdatePassengerProfileRequest): Buffer =>
      Buffer.from(UpdatePassengerProfileRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): UpdatePassengerProfileRequest => UpdatePassengerProfileRequest.decode(value),
    responseSerialize: (value: UpdatePassengerProfileResponse): Buffer =>
      Buffer.from(UpdatePassengerProfileResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): UpdatePassengerProfileResponse =>
      UpdatePassengerProfileResponse.decode(value),
  },
  deletePassengerProfile: {
    path: "/bus.identity.v1.IdentityService/DeletePassengerProfile" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: DeletePassengerProfileRequest): Buffer =>
      Buffer.from(DeletePassengerProfileRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): DeletePassengerProfileRequest => DeletePassengerProfileRequest.decode(value),
    responseSerialize: (value: DeletePassengerProfileResponse): Buffer =>
      Buffer.from(DeletePassengerProfileResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): DeletePassengerProfileResponse =>
      DeletePassengerProfileResponse.decode(value),
  },
} as const;

export interface IdentityServiceServer extends UntypedServiceImplementation {
  health: handleUnaryCall<HealthRequest, HealthResponse>;
  login: handleUnaryCall<LoginRequest, LoginResponse>;
  refresh: handleUnaryCall<RefreshRequest, RefreshResponse>;
  logout: handleUnaryCall<LogoutRequest, LogoutResponse>;
  validateAccessToken: handleUnaryCall<ValidateAccessTokenRequest, ValidateAccessTokenResponse>;
  listPassengerProfiles: handleUnaryCall<ListPassengerProfilesRequest, ListPassengerProfilesResponse>;
  createPassengerProfile: handleUnaryCall<CreatePassengerProfileRequest, CreatePassengerProfileResponse>;
  updatePassengerProfile: handleUnaryCall<UpdatePassengerProfileRequest, UpdatePassengerProfileResponse>;
  deletePassengerProfile: handleUnaryCall<DeletePassengerProfileRequest, DeletePassengerProfileResponse>;
}

export interface IdentityServiceClient extends Client {
  health(
    request: HealthRequest,
    callback: (error: ServiceError | null, response: HealthResponse) => void,
  ): ClientUnaryCall;
  health(
    request: HealthRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: HealthResponse) => void,
  ): ClientUnaryCall;
  health(
    request: HealthRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: HealthResponse) => void,
  ): ClientUnaryCall;
  login(
    request: LoginRequest,
    callback: (error: ServiceError | null, response: LoginResponse) => void,
  ): ClientUnaryCall;
  login(
    request: LoginRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: LoginResponse) => void,
  ): ClientUnaryCall;
  login(
    request: LoginRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: LoginResponse) => void,
  ): ClientUnaryCall;
  refresh(
    request: RefreshRequest,
    callback: (error: ServiceError | null, response: RefreshResponse) => void,
  ): ClientUnaryCall;
  refresh(
    request: RefreshRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: RefreshResponse) => void,
  ): ClientUnaryCall;
  refresh(
    request: RefreshRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: RefreshResponse) => void,
  ): ClientUnaryCall;
  logout(
    request: LogoutRequest,
    callback: (error: ServiceError | null, response: LogoutResponse) => void,
  ): ClientUnaryCall;
  logout(
    request: LogoutRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: LogoutResponse) => void,
  ): ClientUnaryCall;
  logout(
    request: LogoutRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: LogoutResponse) => void,
  ): ClientUnaryCall;
  validateAccessToken(
    request: ValidateAccessTokenRequest,
    callback: (error: ServiceError | null, response: ValidateAccessTokenResponse) => void,
  ): ClientUnaryCall;
  validateAccessToken(
    request: ValidateAccessTokenRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ValidateAccessTokenResponse) => void,
  ): ClientUnaryCall;
  validateAccessToken(
    request: ValidateAccessTokenRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ValidateAccessTokenResponse) => void,
  ): ClientUnaryCall;
  listPassengerProfiles(
    request: ListPassengerProfilesRequest,
    callback: (error: ServiceError | null, response: ListPassengerProfilesResponse) => void,
  ): ClientUnaryCall;
  listPassengerProfiles(
    request: ListPassengerProfilesRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ListPassengerProfilesResponse) => void,
  ): ClientUnaryCall;
  listPassengerProfiles(
    request: ListPassengerProfilesRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ListPassengerProfilesResponse) => void,
  ): ClientUnaryCall;
  createPassengerProfile(
    request: CreatePassengerProfileRequest,
    callback: (error: ServiceError | null, response: CreatePassengerProfileResponse) => void,
  ): ClientUnaryCall;
  createPassengerProfile(
    request: CreatePassengerProfileRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: CreatePassengerProfileResponse) => void,
  ): ClientUnaryCall;
  createPassengerProfile(
    request: CreatePassengerProfileRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: CreatePassengerProfileResponse) => void,
  ): ClientUnaryCall;
  updatePassengerProfile(
    request: UpdatePassengerProfileRequest,
    callback: (error: ServiceError | null, response: UpdatePassengerProfileResponse) => void,
  ): ClientUnaryCall;
  updatePassengerProfile(
    request: UpdatePassengerProfileRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: UpdatePassengerProfileResponse) => void,
  ): ClientUnaryCall;
  updatePassengerProfile(
    request: UpdatePassengerProfileRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: UpdatePassengerProfileResponse) => void,
  ): ClientUnaryCall;
  deletePassengerProfile(
    request: DeletePassengerProfileRequest,
    callback: (error: ServiceError | null, response: DeletePassengerProfileResponse) => void,
  ): ClientUnaryCall;
  deletePassengerProfile(
    request: DeletePassengerProfileRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: DeletePassengerProfileResponse) => void,
  ): ClientUnaryCall;
  deletePassengerProfile(
    request: DeletePassengerProfileRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: DeletePassengerProfileResponse) => void,
  ): ClientUnaryCall;
}

export const IdentityServiceClient = makeGenericClientConstructor(
  IdentityServiceService,
  "bus.identity.v1.IdentityService",
) as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): IdentityServiceClient;
  service: typeof IdentityServiceService;
  serviceName: string;
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
